# React Admin 前端开发规范

## 📋 目录

- [项目概述](#项目概述)
- [技术栈](#技术栈)
- [代码风格规范](#代码风格规范)
- [文件命名规范](#文件命名规范)
- [目录结构规范](#目录结构规范)
- [组件开发规范](#组件开发规范)
- [API 请求规范](#api-请求规范)
- [状态管理规范](#状态管理规范)
- [路由规范](#路由规范)
- [样式规范](#样式规范)
- [Hooks 使用规范](#hooks-使用规范)
- [错误处理规范](#错误处理规范)
- [性能优化规范](#性能优化规范)
- [Git 提交规范](#git-提交规范)
- [代码审查清单](#代码审查清单)

---

## 项目概述

本项目是一个基于 React + Vite + Ant Design 的后台管理系统，采用函数式组件和 Hooks 进行开发，使用 Zustand 进行全局状态管理，通过 Context API 实现表格组件的状态共享。

---

## 技术栈

- **框架**: React 19.x
- **构建工具**: Vite 7.x
- **UI 组件库**: Ant Design 6.x
- **状态管理**: Zustand 5.x
- **路由**: React Router DOM 7.x
- **HTTP 客户端**: Axios 1.x
- **样式预处理**: Less 4.x
- **代码检查**: ESLint 9.x
- **包管理器**: pnpm

---

## 代码风格规范

### 1. 基本规则

- **使用函数式组件**：禁止使用类组件，统一使用函数式组件和 Hooks
- **使用 ES6+ 语法**：优先使用箭头函数、解构赋值、模板字符串等现代语法
- **代码缩进**：使用 4 个空格（不使用 Tab）
- **行尾空格**：删除所有行尾空格
- **文件编码**：统一使用 UTF-8
- **换行符**：统一使用 LF（Unix 风格）

### 2. 命名规范

#### 2.1 变量和函数命名

```javascript
// ✅ 正确：使用小驼峰命名
const userName = 'admin'
const getUserInfo = () => {}
const handleSubmit = () => {}

// ❌ 错误：使用下划线或大驼峰
const user_name = 'admin'
const GetUserInfo = () => {}
```

#### 2.2 组件命名

```javascript
// ✅ 正确：使用大驼峰命名，文件名与组件名保持一致
// 文件：UserModal.jsx
export default function UserModal() {}

// ❌ 错误：组件名与文件名不一致
// 文件：UserModal.jsx
export default function User() {}
```

#### 2.3 常量命名

```javascript
// ✅ 正确：使用大写下划线分隔
const API_BASE_URL = '/api'
const MAX_RETRY_COUNT = 3

// ✅ 正确：配置对象使用大驼峰
const UserConfig = {
  pageSize: 10,
  page: 1
}
```

#### 2.4 类型/类命名

```javascript
// ✅ 正确：使用大驼峰命名
export class UserReqClass {
  username = ''
  page = 1
  page_size = 10
}
```

### 3. 代码格式

#### 3.1 导入顺序

```javascript
// 1. React 相关
import { useState, useEffect, useMemo } from 'react'
import { useNavigate, useLocation } from 'react-router-dom'

// 2. 第三方库
import { Form, Input, Button, message } from 'antd'
import { EditOutlined, DeleteOutlined } from '@ant-design/icons'

// 3. 项目内部组件
import KbpPanel from '@/components/KbpPanel'
import Permission from '@/components/Permission'

// 4. 项目内部工具/API/Hooks
import { useRequest } from '@/hooks/useRequest'
import { apiUserList } from '@/api/user/index'
import { useTable } from '@/context/TableProvider'

// 5. 类型定义
import { UserReqClass } from './types'

// 6. 样式文件（放在最后）
import styles from './index.module.less'
```

#### 3.2 组件结构

```javascript
// ✅ 推荐结构
import React, { useState, useEffect } from 'react'
// ... 其他导入

export default function ComponentName() {
    // 1. Hooks（按使用顺序）
    const [state, setState] = useState()
    const { data } = useCustomHook()
    
    // 2. 计算属性/派生状态
    const computedValue = useMemo(() => {}, [deps])
    
    // 3. 事件处理函数
    const handleClick = () => {}
    
    // 4. 副作用
    useEffect(() => {
        // ...
    }, [deps])
    
    // 5. 渲染
    return <div>...</div>
}
```

#### 3.3 条件渲染

```javascript
// ✅ 推荐：使用三元运算符或逻辑与
{isLoading ? <Loading /> : <Content />}
{hasData && <DataList />}

// ✅ 推荐：复杂条件使用函数
const renderContent = () => {
    if (isLoading) return <Loading />
    if (error) return <Error />
    return <Content />
}

// ❌ 避免：嵌套过深的三元运算符
{condition1 ? (condition2 ? <A /> : <B />) : <C />}
```

#### 3.4 事件处理

```javascript
// ✅ 正确：使用 handle 前缀
const handleSubmit = () => {}
const handleChange = () => {}
const handleDelete = () => {}

// ✅ 正确：内联箭头函数（简单场景）
<Button onClick={() => handleClick(id)}>点击</Button>

// ✅ 正确：复杂逻辑提取函数
const handleEdit = (row) => {
    // 复杂逻辑
    modalRef.current.showModal(row)
}
<Button onClick={() => handleEdit(row)}>编辑</Button>
```

---

## 文件命名规范

### 1. 组件文件

- **页面组件**：使用小驼峰，如 `index.jsx`、`add.jsx`、`edit.jsx`
- **通用组件**：使用大驼峰，如 `KbpPanel/index.jsx`、`UserModal.jsx`
- **组件目录**：使用大驼峰，如 `components/UserModal/`

### 2. 工具文件

- **工具函数**：使用小驼峰，如 `helper.js`、`token.js`、`iconUtils.js`
- **类型定义**：使用小驼峰，如 `types.js`

### 3. API 文件

- **API 文件**：统一使用 `index.js`，放在对应的功能目录下
- **目录命名**：使用小驼峰，如 `api/user/index.js`、`api/system/index.js`

### 4. 样式文件

- **样式文件**：使用 `index.module.less`（CSS Modules）
- **全局样式**：使用 `index.css`

### 5. 配置文件

- **配置文件**：使用小驼峰，如 `vite.config.js`、`eslint.config.js`

---

## 目录结构规范

```
src/
├── api/                    # API 接口定义
│   ├── user/
│   │   └── index.js
│   └── system/
│       └── index.js
├── assets/                 # 静态资源
│   ├── images/
│   └── icons/
├── components/             # 通用组件
│   ├── KbpPanel/
│   │   ├── index.jsx
│   │   └── index.module.less
│   └── Permission/
│       └── index.jsx
├── context/                # Context 定义
│   └── TableProvider.jsx
├── hooks/                  # 自定义 Hooks
│   └── useRequest.js
├── layouts/                # 布局组件
│   ├── index.jsx
│   └── BasicLayout.module.less
├── pages/                  # 页面组件
│   └── permission/
│       └── user/
│           ├── index.jsx       # 列表页
│           ├── add.jsx         # 新增页
│           ├── edit.jsx        # 编辑页
│           ├── types.js        # 类型定义
│           └── components/     # 页面级组件
│               ├── Search.jsx
│               ├── TableList.jsx
│               └── UserModal.jsx
├── router/                 # 路由配置
│   ├── index.jsx
│   └── authRoute.jsx
├── store/                  # 状态管理
│   ├── index.js
│   └── authStore.js
└── utils/                  # 工具函数
    ├── helper.js
    ├── token.js
    └── iconUtils.js
```

### 页面目录结构规范

每个功能模块应遵循以下结构：

```
pages/
└── [module]/              # 模块名（小驼峰）
    └── [feature]/         # 功能名（小驼峰）
        ├── index.jsx      # 列表页主文件
        ├── add.jsx        # 新增页（可选）
        ├── edit.jsx       # 编辑页（可选）
        ├── copy.jsx       # 复制页（可选）
        ├── types.js       # 类型定义和请求类
        └── components/    # 页面级组件
            ├── Search.jsx      # 搜索组件
            ├── TableList.jsx   # 表格组件
            └── [Other].jsx     # 其他组件
```

---

## 组件开发规范

### 1. 组件定义

```javascript
// ✅ 正确：使用函数式组件，导出默认组件
export default function ComponentName() {
    return <div>...</div>
}

// ❌ 错误：使用类组件
class ComponentName extends React.Component {}
```

### 2. Props 类型定义

```javascript
// ✅ 推荐：使用 JSDoc 注释（当前项目未使用 TypeScript）
/**
 * @param {Object} props
 * @param {string} props.title - 标题
 * @param {Function} props.onSubmit - 提交回调
 */
export default function ComponentName({ title, onSubmit }) {
    // ...
}
```

### 3. 组件拆分原则

- **单一职责**：每个组件只负责一个功能
- **可复用性**：通用逻辑提取为独立组件或 Hook
- **页面级组件**：放在 `components/` 目录下
- **业务组件**：放在对应页面的 `components/` 目录下

### 4. 组件通信

```javascript
// ✅ 父子组件：使用 Props
<ChildComponent data={data} onSubmit={handleSubmit} />

// ✅ 跨组件：使用 Context（如 TableProvider）
const { loading, sourceData, onSearch } = useTable()

// ✅ 全局状态：使用 Zustand
const { userInfo, logout } = useAuthStore()
```

### 5. 组件引用

```javascript
// ✅ 正确：使用 @ 别名引用
import KbpPanel from '@/components/KbpPanel'
import { useRequest } from '@/hooks/useRequest'
import { apiUserList } from '@/api/user/index'

// ❌ 错误：使用相对路径（超过两层）
import KbpPanel from '../../../components/KbpPanel'
```

### 6. 组件插槽模式

```javascript
// ✅ 使用组合模式实现插槽（参考 KbpPanel）
export default function KbpPanel({ children }) {
    const Search = children.find(child => child.type === KbpPanel.Search)
    const Table = children.find(child => child.type === KbpPanel.Table)
    return (
        <section>
            {Search?.props?.children}
            {Table?.props?.children}
        </section>
    )
}
KbpPanel.Search = 'Search'
KbpPanel.Table = 'Table'

// 使用方式
<KbpPanel>
    <KbpPanel.Search>
        <Search />
    </KbpPanel.Search>
    <KbpPanel.Table>
        <TableList />
    </KbpPanel.Table>
</KbpPanel>
```

---

## API 请求规范

### 1. API 文件组织

```javascript
// api/user/index.js
import request from '@/api/request'

// ✅ 统一导出 API 函数，使用 api 前缀
export const apiUserList = (params) => {
    return request({
        url: '/user/list',
        method: 'get',
        params
    })
}

export const apiUserAdd = (data) => {
    return request({
        url: '/user/add',
        method: 'post',
        data
    })
}

export const apiUserDelete = (ids) => {
    return request({
        url: '/user/delete',
        method: 'post',
        data: { ids }
    })
}
```

### 2. 请求参数类定义

```javascript
// pages/user/types.js
// ✅ 使用类定义请求参数，便于初始化和类型管理
export class UserReqClass {
    username = ''
    nickname = ''
    page = 1
    page_size = 10
}

// 使用方式
const props = useRequest(apiUserList, new UserReqClass())
```

### 3. 响应数据格式

```javascript
// ✅ 统一响应格式
{
    code: 200,        // 状态码，200 表示成功
    message: '成功',  // 提示信息
    data: {           // 数据
        list: [],     // 列表数据
        total: 0      // 总数
    }
}
```

### 4. 错误处理

```javascript
// ✅ 在 useRequest Hook 中统一处理
// ✅ 在 API 拦截器中统一处理（request.js）
// ✅ 特殊错误在组件中处理
try {
    const res = await apiUserDelete([row.id])
    if (res.code != 200) {
        return Promise.reject()
    }
    message.success('删除成功！')
} catch (error) {
    // 错误已在拦截器中处理
}
```

---

## 状态管理规范

### 1. 全局状态（Zustand）

```javascript
// store/authStore.js
import { create } from 'zustand'

export const useAuthStore = create((set) => ({
    token: '',
    userInfo: null,
    menus: [],
    setToken: (token) => set({ token }),
    setUserInfo: (userInfo) => set({ userInfo }),
    logout: () => set({ token: '', userInfo: null, menus: [] })
}))

// 使用方式
const { userInfo, logout } = useAuthStore()
```

### 2. 局部状态（useState）

```javascript
// ✅ 简单状态使用 useState
const [loading, setLoading] = useState(false)
const [visible, setVisible] = useState(false)
```

### 3. 派生状态（useMemo/useCallback）

```javascript
// ✅ 计算属性使用 useMemo
const filteredData = useMemo(() => {
    return data.filter(item => item.status === 1)
}, [data])

// ✅ 函数记忆化使用 useCallback
const handleClick = useCallback(() => {
    // ...
}, [deps])
```

### 4. 表格状态（Context API）

```javascript
// ✅ 使用 TableProvider 管理表格状态
import { TableProvider } from '@/context/TableProvider'
import { useRequest } from '@/hooks/useRequest'

const props = useRequest(apiUserList, new UserReqClass())
return (
    <TableProvider {...props}>
        <TableList />
    </TableProvider>
)

// 在子组件中使用
const { loading, sourceData, onSearch } = useTable()
```

---

## 路由规范

### 1. 路由配置

```javascript
// router/index.jsx
// ✅ 使用懒加载
const User = lazy(() => import('@/pages/permission/user'))

// ✅ 路由结构
{
    path: '/permission/user',
    element: <AuthRoute><User /></AuthRoute>,
    meta: {
        title: '用户管理',
        hidden: false
    }
}
```

### 2. 路由守卫

```javascript
// router/authRoute.jsx
// ✅ 使用 AuthRoute 组件进行权限控制
<AuthRoute>
    <Component />
</AuthRoute>
```

### 3. 路由跳转

```javascript
// ✅ 使用 useNavigate Hook
import { useNavigate } from 'react-router-dom'

const navigate = useNavigate()
navigate('/permission/user')
navigate('/permission/user', { state: { id: 1 } })
```

---

## 样式规范

### 1. CSS Modules

```javascript
// ✅ 使用 CSS Modules，避免样式污染
import styles from './index.module.less'

<div className={styles.container}>
    <div className={styles.header}>标题</div>
</div>
```

### 2. Less 文件组织

```less
// index.module.less
// ✅ 使用 BEM 命名规范（可选，但保持一致性）
.container {
    padding: 16px;
    
    &-header {
        font-size: 16px;
        font-weight: bold;
    }
    
    &-content {
        margin-top: 16px;
    }
}
```

### 3. 全局样式

```less
// index.css
// ✅ 只定义全局重置样式和通用类
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

.mb-12 {
    margin-bottom: 12px;
}
```

### 4. 样式变量

```less
// ✅ 使用 Less 变量定义主题色
@primary-color: #1890ff;
@success-color: #52c41a;
@error-color: #ff4d4f;
```

---

## Hooks 使用规范

### 1. 自定义 Hooks

```javascript
// hooks/useRequest.js
// ✅ 以 use 开头命名
export const useRequest = (apiFun, requestParams, arg = { immediate: true }) => {
    const [loading, setLoading] = useState(false)
    // ...
    return { loading, sourceData, onSearch }
}
```

### 2. Hooks 使用规则

```javascript
// ✅ 1. 只在组件顶层调用
// ✅ 2. 只在 React 函数中调用
// ✅ 3. 按顺序调用，不要在条件语句中调用

// ❌ 错误
if (condition) {
    const [state, setState] = useState()
}

// ✅ 正确
const [state, setState] = useState()
if (condition) {
    // 使用 state
}
```

### 3. 常用 Hooks

```javascript
// ✅ useState：状态管理
const [count, setCount] = useState(0)

// ✅ useEffect：副作用处理
useEffect(() => {
    // 组件挂载或依赖变化时执行
    return () => {
        // 清理函数
    }
}, [deps])

// ✅ useMemo：计算属性缓存
const value = useMemo(() => computeExpensiveValue(a, b), [a, b])

// ✅ useCallback：函数缓存
const callback = useCallback(() => {
    // ...
}, [deps])

// ✅ useRef：引用 DOM 或值
const inputRef = useRef(null)
const countRef = useRef(0)
```

---

## 错误处理规范

### 1. API 错误处理

```javascript
// ✅ 在 request.js 拦截器中统一处理
request.interceptors.response.use(
    response => {
        const { data } = response
        if (data.code != 200) {
            return Promise.reject(new Error(data.message || '请求失败'))
        }
        return data
    },
    error => {
        // 统一处理 HTTP 错误
        if (error.response) {
            const { status } = error.response
            switch (status) {
                case 401:
                    message.error('登录已过期，请重新登录')
                    // 跳转登录
                    break
                case 403:
                    message.error('没有权限访问')
                    break
                // ...
            }
        }
        return Promise.reject(error)
    }
)
```

### 2. 组件错误处理

```javascript
// ✅ 使用 try-catch 处理异步操作
const handleDelete = async (row) => {
    Modal.confirm({
        title: '提示',
        content: '是否删除数据？',
        onOk: async () => {
            try {
                const { code } = await apiUserDelete([row.id])
                if (code != 200) {
                    return Promise.reject()
                }
                message.success('删除成功！')
                onSearch()
            } catch (error) {
                // 错误已在拦截器中处理
            }
        }
    })
}
```

### 3. 表单验证

```javascript
// ✅ 使用 Ant Design Form 的验证规则
<Form.Item
    label="用户名"
    name="username"
    rules={[
        { required: true, message: '请输入用户名' },
        { min: 3, message: '用户名至少3个字符' }
    ]}
>
    <Input placeholder="请输入用户名" />
</Form.Item>
```

---

## 性能优化规范

### 1. 组件优化

```javascript
// ✅ 使用 React.memo 优化组件渲染
const MemoizedComponent = React.memo(({ data }) => {
    return <div>{data}</div>
})

// ✅ 使用 useMemo 缓存计算结果
const expensiveValue = useMemo(() => {
    return computeExpensiveValue(data)
}, [data])

// ✅ 使用 useCallback 缓存函数
const handleClick = useCallback(() => {
    // ...
}, [deps])
```

### 2. 列表渲染优化

```javascript
// ✅ 使用 key 属性（Ant Design Table 自动处理）
<Table
    rowKey={(record) => record.id}
    dataSource={sourceData}
    columns={columns}
/>

// ✅ 虚拟滚动（Ant Design Table 支持）
<Table
    scroll={{ y: 400 }}
    // ...
/>
```

### 3. 代码分割

```javascript
// ✅ 路由懒加载
import { lazy } from 'react'
const User = lazy(() => import('@/pages/permission/user'))

// ✅ 组件懒加载
const HeavyComponent = lazy(() => import('./HeavyComponent'))
```

### 4. 图片优化

```javascript
// ✅ 使用合适的图片格式和大小
// ✅ 使用懒加载
<img src={src} loading="lazy" alt="描述" />
```

---

## Git 提交规范

### 1. 提交信息格式

```
<type>(<scope>): <subject>

<body>

<footer>
```

### 2. Type 类型

- `feat`: 新功能
- `fix`: 修复 bug
- `docs`: 文档更新
- `style`: 代码格式调整（不影响功能）
- `refactor`: 代码重构
- `perf`: 性能优化
- `test`: 测试相关
- `chore`: 构建过程或辅助工具的变动

### 3. 提交示例

```bash
# ✅ 正确示例
feat(user): 添加用户管理页面
fix(table): 修复表格分页问题
docs(readme): 更新项目说明文档
refactor(api): 重构 API 请求封装

# ❌ 错误示例
update code
fix bug
```

### 4. 分支命名

- `feature/功能名称`：新功能分支
- `fix/问题描述`：bug 修复分支
- `hotfix/紧急修复`：紧急修复分支
- `refactor/重构内容`：重构分支

---

## 代码审查清单

在提交代码前，请检查以下事项：

### 功能检查
- [ ] 功能是否按需求实现
- [ ] 边界情况是否处理
- [ ] 错误处理是否完善

### 代码质量
- [ ] 代码是否符合命名规范
- [ ] 代码是否有重复逻辑（需要提取）
- [ ] 组件是否拆分合理
- [ ] 是否有未使用的导入或变量

### 性能检查
- [ ] 是否有不必要的重渲染
- [ ] 是否使用了 useMemo/useCallback 优化
- [ ] 列表渲染是否使用了 key

### 样式检查
- [ ] 是否使用了 CSS Modules
- [ ] 样式是否响应式适配
- [ ] 是否遵循设计规范

### 测试检查
- [ ] 功能是否测试通过
- [ ] 是否影响其他功能

### 文档检查
- [ ] 复杂逻辑是否有注释
- [ ] 组件 Props 是否有说明（JSDoc）

---

## 常见问题 FAQ

### Q1: 如何新增一个功能模块？

1. 在 `pages/` 下创建模块目录和功能目录
2. 创建 `index.jsx`（列表页）、`add.jsx`（新增页）、`edit.jsx`（编辑页）
3. 创建 `types.js` 定义请求参数类
4. 创建 `components/` 目录，添加 `Search.jsx` 和 `TableList.jsx`
5. 在 `api/` 下创建对应的 API 文件
6. 在路由中添加路由配置

### Q2: 如何使用表格组件？

```javascript
// 1. 使用 useRequest Hook
const props = useRequest(apiUserList, new UserReqClass())

// 2. 使用 TableProvider 包裹
<TableProvider {...props}>
    <KbpPanel>
        <KbpPanel.Search>
            <Search />
        </KbpPanel.Search>
        <KbpPanel.Table>
            <TableList />
        </KbpPanel.Table>
    </KbpPanel>
</TableProvider>

// 3. 在子组件中使用
const { loading, sourceData, onSearch } = useTable()
```

### Q3: 如何实现权限控制？

```javascript
// 使用 Permission 组件包裹需要权限控制的按钮
<Permission pers={['config:config:add']}>
    <Button type="primary" onClick={handleAdd}>新增</Button>
</Permission>
```

### Q4: 如何处理表单提交？

```javascript
// 1. 使用 Ant Design Form
const [form] = Form.useForm()

// 2. 提交时获取表单值
const handleSubmit = async () => {
    const values = await form.validateFields()
    const res = await apiUserAdd(values)
    if (res.code === 200) {
        message.success('添加成功')
        // 刷新列表或跳转
    }
}
```

---

## 更新日志

- **2024-XX-XX**: 初始版本，制定基础开发规范

---

## 参考资源

- [React 官方文档](https://react.dev/)
- [Ant Design 官方文档](https://ant.design/)
- [Vite 官方文档](https://vitejs.dev/)
- [Zustand 官方文档](https://zustand-demo.pmnd.rs/)

---

**注意**：本规范会根据项目发展持续更新，请定期查看最新版本。


##===============================================================================

# 微信抢红包 Auto.js 脚本使用说明

## 📋 目录
1. [准备工作](#准备工作)
2. [安装步骤](#安装步骤)
3. [脚本配置](#脚本配置)
4. [使用方法](#使用方法)
5. [功能说明](#功能说明)
6. [常见问题](#常见问题)
7. [注意事项](#注意事项)

---

## 🔧 准备工作

### 1. 设备要求
- Android 手机（版本 5.0 及以上）
- 已安装微信应用
- 手机已 root（推荐，非必需）

### 2. 需要安装的软件
- **Auto.js** 应用（推荐版本 4.1.1 或更高）
  - 下载地址：GitHub 搜索 "hyb1996/Auto.js"
  - 或使用 Auto.js Pro（付费版本，功能更强大）

---

## 📥 安装步骤

### 步骤 1: 安装 Auto.js
1. 下载 Auto.js APK 文件
2. 在手机上安装 APK（需要允许"未知来源"安装）
3. 打开 Auto.js 应用

### 步骤 2: 授予必要权限
1. **无障碍服务权限**（必需）
   - 打开手机"设置" → "辅助功能" → "无障碍"
   - 找到"Auto.js"或"无障碍服务"
   - 开启开关
   - 在 Auto.js 应用中确认权限已授予

2. **悬浮窗权限**（可选，用于显示调试信息）
   - 打开手机"设置" → "应用管理" → "Auto.js"
   - 找到"悬浮窗"或"显示在其他应用的上层"
   - 开启权限

3. **后台运行权限**（推荐）
   - 在手机设置中，将 Auto.js 加入白名单
   - 允许后台运行和自启动

### 步骤 3: 导入脚本
1. 将 `wechat-redpacket-auto.js` 文件复制到手机
2. 在 Auto.js 应用中：
   - 点击右上角"+"号
   - 选择"导入文件"
   - 选择脚本文件
   - 或直接创建新脚本，复制代码内容

---

## ⚙️ 脚本配置

打开脚本文件，找到 `CONFIG` 配置对象，可以修改以下参数：

```javascript
const CONFIG = {
    clickDelay: 200,        // 点击延迟（毫秒），建议 100-500
    checkInterval: 500,     // 检测间隔（毫秒），建议 300-1000
    debug: true,            // 是否显示调试日志
    redPacketKeywords: [...], // 红包关键词（一般不需要修改）
    autoBack: false,        // 是否自动返回（暂未实现）
    onlyGroupChat: false    // 是否只抢群聊（暂未实现）
};
```

### 参数说明：
- **clickDelay**: 点击红包后的延迟时间，太快可能被微信检测，太慢可能抢不到
- **checkInterval**: 检测红包的频率，频率越高消耗资源越多
- **debug**: 开启后会显示详细的运行日志，方便调试

---

## 🚀 使用方法

### 方法一：手动运行
1. 打开 Auto.js 应用
2. 找到"微信抢红包"脚本
3. 点击运行按钮（▶️）
4. 脚本会自动打开微信
5. **手动进入聊天界面**（群聊或私聊）
6. 脚本开始自动检测和抢红包

### 方法二：定时运行
1. 在 Auto.js 中设置定时任务
2. 选择脚本和运行时间
3. 脚本会在指定时间自动运行

### 停止脚本
- 在 Auto.js 应用中点击"停止"按钮
- 或按音量下键快速停止（需要在设置中开启）

---

## 🎯 功能说明

### 核心功能
1. **自动检测红包**
   - 通过多种方式识别红包消息
   - 支持文本、描述、ID、类名等多种查找方式

2. **自动点击红包**
   - 检测到红包后自动点击
   - 智能定位红包位置

3. **自动打开红包**
   - 点击红包后自动点击"开"按钮
   - 支持多种定位方式

4. **自动关闭详情页**
   - 抢完红包后自动返回聊天界面
   - 继续监听下一个红包

### 工作流程
```
启动脚本 → 打开微信 → 进入聊天界面 → 持续监听
    ↓
检测红包 → 点击红包 → 点击"开"按钮 → 查看结果
    ↓
关闭详情页 → 继续监听
```

---

## ❓ 常见问题

### Q1: 脚本无法找到红包？
**解决方案：**
1. 检查无障碍服务是否已开启
2. 确认当前在聊天界面（不是微信主界面）
3. 尝试调整 `checkInterval` 参数，增加检测频率
4. 检查微信版本是否过新（可能需要更新脚本）

### Q2: 点击红包后没有反应？
**解决方案：**
1. 增加 `clickDelay` 延迟时间
2. 检查手机性能，确保运行流畅
3. 尝试手动点击一次红包，观察脚本日志

### Q3: 脚本运行一段时间后停止？
**解决方案：**
1. 检查手机是否进入省电模式
2. 将 Auto.js 加入白名单
3. 关闭手机的后台清理功能

### Q4: 抢红包速度太慢？
**解决方案：**
1. 减小 `clickDelay` 和 `checkInterval` 参数
2. 关闭调试日志（设置 `debug: false`）
3. 确保手机性能良好

### Q5: 微信提示"操作过于频繁"？
**解决方案：**
1. 增加 `clickDelay` 延迟时间
2. 增加 `checkInterval` 检测间隔
3. 适当降低抢红包频率

### Q6: 脚本在不同手机上不兼容？
**解决方案：**
1. 不同手机分辨率不同，可能需要调整坐标
2. 不同微信版本界面可能不同
3. 查看日志，根据实际情况修改查找方式

---

## ⚠️ 注意事项

### 重要警告
1. **使用风险**
   - 使用自动化脚本可能违反微信服务条款
   - 存在账号被封禁的风险
   - 请谨慎使用，建议仅用于学习和测试

2. **法律风险**
   - 本脚本仅供学习交流使用
   - 不得用于商业用途
   - 使用者需自行承担所有风险

3. **技术限制**
   - 脚本可能因微信更新而失效
   - 不同手机和微信版本可能需要调整
   - 无法保证 100% 成功率

### 使用建议
1. **适度使用**
   - 不要设置过快的抢红包速度
   - 避免长时间连续运行
   - 建议设置合理的延迟时间

2. **定期更新**
   - 微信更新后可能需要更新脚本
   - 关注 Auto.js 社区的最新解决方案

3. **备份账号**
   - 使用前备份重要聊天记录
   - 准备备用微信账号

4. **测试环境**
   - 先在测试群中试用
   - 确认脚本正常工作后再正式使用

---

## 🔄 脚本更新日志

### v1.0.0 (初始版本)
- 实现基本的红包检测功能
- 实现自动点击红包功能
- 实现自动打开红包功能
- 添加调试日志功能
- 添加配置参数

---

## 📚 进阶使用

### 自定义红包识别
如果默认的红包识别方式不工作，可以修改 `findRedPacket()` 函数：

```javascript
function findRedPacket() {
    // 添加自定义查找逻辑
    // 例如：通过特定颜色、位置等
}
```

### 添加通知功能
可以在抢到红包后发送通知：

```javascript
function notifyRedPacket() {
    engines.myEngine().notify('抢到红包！');
}
```

### 添加统计功能
可以记录抢红包的次数和成功率：

```javascript
let stats = {
    total: 0,
    success: 0,
    failed: 0
};
```

---

## 📞 技术支持

如果遇到问题：
1. 查看 Auto.js 官方文档
2. 在 Auto.js 社区寻求帮助
3. 检查脚本日志，定位问题

---

## 📄 免责声明

本脚本仅供学习和研究使用，使用者需自行承担使用风险。作者不对因使用本脚本导致的任何损失负责。

---

**最后更新：2024年**


/**
 * 微信抢红包自动化脚本
 * 基于 auto.js 实现
 * 
 * 使用说明：
 * 1. 确保已安装 auto.js 应用
 * 2. 授予无障碍服务权限
 * 3. 授予悬浮窗权限（可选）
 * 4. 运行此脚本
 */

// ==================== 配置参数 ====================
const CONFIG = {
    // 抢红包延迟时间（毫秒），建议 100-500ms
    clickDelay: 200,
    // 检测红包的间隔时间（毫秒）
    checkInterval: 500,
    // 是否开启调试日志
    debug: true,
    // 红包关键词（用于识别红包）
    redPacketKeywords: ['微信红包', '红包', '领取红包'],
    // 是否自动返回聊天列表
    autoBack: false,
    // 是否只抢群聊红包
    onlyGroupChat: false
};

// ==================== 工具函数 ====================
function log(message) {
    if (CONFIG.debug) {
        console.log(`[${new Date().toLocaleTimeString()}] ${message}`);
    }
}

function sleep(ms) {
    return new Promise(resolve => setTimeout(resolve, ms));
}

// ==================== 核心功能函数 ====================

/**
 * 检查当前是否在微信界面
 */
function isWeChatOpen() {
    const currentPackage = currentPackage();
    return currentPackage === 'com.tencent.mm';
}

/**
 * 打开微信应用
 */
async function openWeChat() {
    log('正在打开微信...');
    app.launch('com.tencent.mm');
    await sleep(2000);

    if (!isWeChatOpen()) {
        throw new Error('无法打开微信应用');
    }
    log('微信已打开');
}

/**
 * 查找红包控件
 * 根据不同的微信版本，红包控件的特征可能不同
 */
function findRedPacket() {
    // 方法1: 通过文本查找
    const textRedPacket = textMatches(/.*红包.*/).findOne(1000);
    if (textRedPacket) {
        log('通过文本找到红包');
        return textRedPacket;
    }

    // 方法2: 通过描述查找
    const descRedPacket = descMatches(/.*红包.*/).findOne(1000);
    if (descRedPacket) {
        log('通过描述找到红包');
        return descRedPacket;
    }

    // 方法3: 通过ID查找（不同微信版本ID可能不同）
    const idRedPacket = id('com.tencent.mm:id/').findOne(1000);
    if (idRedPacket) {
        log('通过ID找到红包');
        return idRedPacket;
    }

    // 方法4: 通过类名查找
    const classNameRedPacket = className('android.widget.TextView')
        .textMatches(/.*红包.*/)
        .findOne(1000);
    if (classNameRedPacket) {
        log('通过类名找到红包');
        return classNameRedPacket;
    }

    return null;
}

/**
 * 点击红包
 */
async function clickRedPacket(redPacket) {
    if (!redPacket) {
        return false;
    }

    try {
        log('发现红包，准备点击...');

        // 获取红包位置
        const bounds = redPacket.bounds();
        const x = bounds.centerX();
        const y = bounds.centerY();

        // 点击红包
        click(x, y);
        log(`已点击红包，位置: (${x}, ${y})`);

        await sleep(CONFIG.clickDelay);
        return true;
    } catch (e) {
        log(`点击红包失败: ${e.message}`);
        return false;
    }
}

/**
 * 查找并点击"开"按钮
 */
async function clickOpenButton() {
    log('正在查找"开"按钮...');

    // 等待红包详情页面加载
    await sleep(500);

    // 方法1: 通过文本查找"开"按钮
    const openButton = text('开').findOne(2000);
    if (openButton) {
        log('找到"开"按钮，准备点击');
        openButton.click();
        await sleep(300);
        return true;
    }

    // 方法2: 通过描述查找
    const openButtonByDesc = desc('开').findOne(2000);
    if (openButtonByDesc) {
        log('通过描述找到"开"按钮');
        openButtonByDesc.click();
        await sleep(300);
        return true;
    }

    // 方法3: 通过坐标点击（不同手机分辨率需要调整）
    // 红包详情页的"开"按钮通常在屏幕下方中央位置
    const screenWidth = device.width;
    const screenHeight = device.height;
    const openButtonX = screenWidth / 2;
    const openButtonY = screenHeight * 0.75; // 屏幕下方75%位置

    log(`尝试点击坐标: (${openButtonX}, ${openButtonY})`);
    click(openButtonX, openButtonY);
    await sleep(300);

    return true;
}

/**
 * 关闭红包详情页面
 */
async function closeRedPacketDetail() {
    log('关闭红包详情页面...');

    // 按返回键
    back();
    await sleep(500);

    // 如果还在详情页，再按一次返回键
    if (text('开').exists()) {
        back();
        await sleep(500);
    }
}

/**
 * 检查是否抢到红包
 */
function checkRedPacketResult() {
    // 检查是否有"已领取"、"手慢了"等提示
    const resultTexts = ['已领取', '手慢了', '已抢完', '已过期'];

    for (let text of resultTexts) {
        if (text(text).exists()) {
            log(`红包结果: ${text}`);
            return true;
        }
    }

    return false;
}

/**
 * 主循环：持续监听红包
 */
async function mainLoop() {
    log('开始监听红包...');

    while (true) {
        try {
            // 检查是否在微信界面
            if (!isWeChatOpen()) {
                log('不在微信界面，等待中...');
                await sleep(2000);
                continue;
            }

            // 查找红包
            const redPacket = findRedPacket();

            if (redPacket) {
                log('发现红包！');

                // 点击红包
                const clicked = await clickRedPacket(redPacket);

                if (clicked) {
                    // 点击"开"按钮
                    await clickOpenButton();

                    // 检查结果
                    await sleep(500);
                    checkRedPacketResult();

                    // 关闭详情页
                    await closeRedPacketDetail();

                    log('完成一次抢红包操作');
                }
            } else {
                // 没有找到红包，继续监听
                log('未发现红包，继续监听...');
            }

            // 等待一段时间后继续检测
            await sleep(CONFIG.checkInterval);

        } catch (e) {
            log(`发生错误: ${e.message}`);
            await sleep(1000);
        }
    }
}

/**
 * 初始化函数
 */
async function init() {
    log('========== 微信抢红包脚本启动 ==========');
    log(`配置信息: ${JSON.stringify(CONFIG, null, 2)}`);

    // 请求无障碍服务权限
    if (!auto.service) {
        log('请求无障碍服务权限...');
        auto.waitFor();
    }

    // 请求悬浮窗权限（可选）
    if (!floaty.checkPermission()) {
        log('请求悬浮窗权限...');
        floaty.requestPermission();
    }

    // 打开微信
    await openWeChat();

    // 等待用户进入聊天界面
    log('请进入聊天界面，脚本将在3秒后开始运行...');
    await sleep(3000);

    // 开始主循环
    await mainLoop();
}

// ==================== 启动脚本 ====================
// 运行初始化函数
init().catch(error => {
    log(`脚本运行出错: ${error.message}`);
    console.error(error);
});

